<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="res_users_form_inherit_lms_payment" model="ir.ui.view">
        <field name="name">res.users.form.inherit</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">

            <xpath expr="//notebook" position="inside">
                <page string="Payments">
                    <group string="Payment Info">
                        <field name="total_paid_student" invisible="user_type != 'student'" readonly="1" widget="monetary"/>
                        <field name="total_paid_teacher" invisible="user_type != 'teacher'" readonly="1" widget="monetary"/>
                    </group>
                    <field name="student_payment_ids" context="{'default_user_id': id}"
                        invisible="user_type != 'student'">
                        <list editable="bottom">
                            <field name="payment_date"/>
                            <field name="amount" widget="monetary"/>
                        </list>
                    </field>
                    <field name="teacher_payment_ids" context="{'default_user_id': id}"
                        invisible="user_type != 'teacher'">
                        <list editable="bottom">
                            <field name="payment_date"/>
                            <field name="amount" widget="monetary"/>
                        </list>
                    </field>
                </page>
            </xpath>

            <xpath expr="//div[@name='button_box']" position="inside">
                <div class="oe_button_box" name="button_box">
                    <button name="action_balance"
                            type="object"
                            class="oe_stat_button"
                            icon="fa-money"
                            string="Balance">
                        <field name="balance" widget="statinfo"/>
                    </button>
                </div>
            </xpath>

        </field>
    </record>
</odoo>
